# Normalize line endings — checked into the repo as LF, checked out
# native on Windows. Avoids CRLF noise across mixed-OS contributors and CI.
* text=auto eol=lf

# Binary asset types — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.wasm binary
